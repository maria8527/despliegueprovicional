import { ObjectId } from "mongodb";

export default interface score {
     id: string;                                                                                                                                    scoress: number;
     pago_finalizado: number;
}